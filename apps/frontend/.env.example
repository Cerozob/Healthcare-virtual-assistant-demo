# ⚠️  IMPORTANT: This project does NOT use .env files!
# 
# All configuration is handled through Amplify secrets and outputs.
# See AMPLIFY_SECRETS.md for configuration instructions.
#
# This file is kept for reference only and should not be used.

# Configuration is now handled via:
# 1. Amplify secrets (set in Amplify Console)
# 2. amplify_outputs.json (generated by Amplify)
# 3. Dynamic configuration at runtime

# Required secrets in Amplify Console:
# - CDK_API_GATEWAY_ENDPOINT
# - CDK_S3_BUCKET_NAME  
# - AWS_REGION
